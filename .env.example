# ========================
# Archivo: .env.example
# ========================
# Copia este archivo a ".env" antes de correr el proyecto:
#   cp .env.example .env
# Luego puedes ajustar valores si lo necesitas.

# --- Postgres (Base de datos) ---
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=mydb

# --- SQLAlchemy connection string (para FastAPI) ---
# Nota: el host debe coincidir con el nombre del servicio en docker-compose (fastapi_books_db)
DATABASE_URL=postgresql+psycopg2://user:password@fastapi_books_db:5432/mydb

# --- Google Books API ---
# URL base para consultas a Google Books
GOOGLE_BOOKS_API_URL=https://www.googleapis.com/books/v1/volumes
